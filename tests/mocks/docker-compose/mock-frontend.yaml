version: '3'

services:
  frontend:
    name: "frontend-application"
    ports: 
      - "3000:3000"
    environment: 
      BACKEND_API_URL: "http://backend-application:8080"
      DEBUG: "true"
    deploy: 
      replicas: 3
      